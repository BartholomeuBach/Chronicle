{
  "name": "chronicle-ai-dungeon",
  "version": "0.1.0",
  "private": true,
  "description": "Chronicle development and AI Dungeon packaging tooling.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:dist": "node tools/verify-dist.mjs",
    "check": "npm run typecheck && npm run test && npm run build && npm run verify:dist"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
