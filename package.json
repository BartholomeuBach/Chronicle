{
  "name": "chronicle-ai-dungeon",
  "version": "0.1.0",
  "private": true,
  "description": "Chronicle development and AI Dungeon packaging tooling.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:dist": "node tools/verify-dist.mjs",
    "verify:dist:fresh": "node tools/verify-dist-fresh.mjs",
    "verify:embedded-snippets": "node tools/verify-embedded-snippets.mjs",
    "sync:embedded-snippets": "node tools/sync-embedded-snippets.mjs",
    "check": "npm run typecheck && npm run test && npm run build && npm run verify:dist && npm run verify:dist:fresh && npm run verify:embedded-snippets"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
